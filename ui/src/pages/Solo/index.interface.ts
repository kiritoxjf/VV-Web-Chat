export interface iInfo {
  name: string
  avatar: string
}

export interface iCall {
  id: string
}

export interface iDevice {
  deviceId: string
  groupId: string
  kind: string
  label: string
}
